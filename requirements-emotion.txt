# Python dependencies for audio emotion analysis
#
# Setup (recommended - uses virtual environment):
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements-emotion.txt
#
# Or system-wide (requires --break-system-packages on newer Debian/Ubuntu):
#   python3 -m pip install --break-system-packages -r requirements-emotion.txt

transformers>=4.30.0
torch>=2.0.0
torchaudio>=2.0.0
librosa>=0.10.0
soundfile>=0.12.0
protobuf>=3.20.0
